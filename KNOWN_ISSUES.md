# KNOWN ISSUES — self-review такта 2 (этап 0, сессия 1)

> Прогон по протоколу lynn-review с нарушенным принципом №1 (ревьюер = сессия-автор).
> Все находки получены независимыми путями: отдельный чекер контента (не парсеры сборки),
> поглазная сверка сканов, grep по источнику. Полноценный гейт — fresh-session бриф в
> `docs/lynn_review_brief_stage0_session1.md`.

## Блокеров нет. Non-blocking findings:

### F1 · MINOR · [ИСПРАВЛЕНО 11.06] · shadow_recovery.json — пересказ вместо дословного текста
Строки `condition` переформулированы из условных предложений книги
(«Если действия **вашего** героя хотя бы немного помешали возвращению Тени,
**уберите 1 балл Тени**») в декларативы. Числа и смысл верны (`shadow_removed`
хранится отдельно и сверен со сканом стр. 22), но нарушена политика
«исходный текст хранится дословно». Чинить при верификации: вернуть
формулировки книги в `condition` или добавить поле `source_text`.
Обнаружено независимым чекером (3 несовпадения из 215 ячеек).

### F2 · MINOR · [ИСПРАВЛЕНО 11.06] · VERIFICATION_REPORT.md, пункт 2 — ложное утверждение о диффе
Отчёт утверждает дифф «достать/найти» у Бильбо-1. По факту: в обоих источниках
(стр. 8 и 29, grep = 0 вхождений «достать») стоит «найти»; «достать» появилось
только в моём примере схемы такта 1 — дописанная по памяти обрезанная цитата.
JSON корректен; отчёт вводит в заблуждение. Урок шире находки: цитата,
восстановленная по памяти, просочилась в рабочий документ несмотря на
анти-паттерн №1 проекта.

### F3 · INFO · [УЗАКОНЕНО Иваном 11.06] · journey_scenes.json — поле `skills_pool` сверх утверждённой схемы
Добавлено при сборке (список ИССЛЕДОВАНИЕ/БДИТЕЛЬНОСТЬ/ОХОТА со стр. 19),
в отличие от `fatigue_waived` не было задекларировано при сдаче. Полезно движку,
источнику соответствует, но это молчаливое расширение схемы. Решение за Иваном:
оставить (тогда внести в схему такта 1 задним числом) или убрать.

### F4 · INFO · [ИСПРАВЛЕНО 11.06] · parsers.py:69 — мёртвая функция `parse_feat_event_rows`
Заброшенный черновик с `raise NotImplementedError`. Задекларировано при сдаче,
подтверждено ревью. Удалить при следующем касании файла.

### F5 · INFO · текстовый слой может терять слова — диффы перепроверять по сканам
Дифф «Детали сцены: Неприятность», face 4 («выследить добычу» vs «выследить»)
зафиксирован по текстовому слою; скан стр. 27 в этой строке требует лупы.
При верификации диффов сверяться со сканами/бумагой, не с текстовым слоем.

## Подтверждено ревью (выборка ключевых улик)

- Канон рун: КВ, «выполнение проверок»: «на двенадцатигранниках (Костях
  испытания) цифра 11 — это Око Саурона, а цифра 12 — руна Гэндальфа» (grep).
- Знаки эффектов: удача/Око = −1, неудача/Око = +2, short_cut = −1 день,
  mishap = +1 день — сверены со сканами стр. 19, 23.
- 215/215 текстовых ячеек (кроме F1) найдены в заявленных страницах источника
  независимым чекером.
- Поглазно сверены сканы: 7, 8, 9, 11, 12*, 13, 16, 19, 20, 21, 22, 23, 26, 27, 28*
  (* — частично, в составе других проверок). Без глаз остаются: стр. 6
  (15 баллов опыта, братство 3 — текст однозначен), стр. 29–30 (приложение,
  вторичный источник).
- Валидатор: 25/25 файлов, схемы + структурные инварианты — зелёный (повторный прогон).
- Скоуп: 25 JSON + 9 схем + tools/extraction + 2 отчёта; engine/ не существует,
  контент в код не просочился.

## Из гейтов сессии 2 (vision GREEN, lynn-review PASS, 11.06)

- **KI-D · MEDIUM · [ИСПРАВЛЕНО]** — mark_verified.py параметризован (--dir, --gate2, --gate3); хардкод сессии 1 убран.
- **KI-E · LOW · [УТОЧНЕНО]** — PROVENANCE: 54 уникальных пары = 55 вхождений; оба числа верны, формулировка снята.
- **KI-F · LOW · [ИСПРАВЛЕНО]** — докстринг validate.py приведён к ADR-001.
- **KI-G · LOW · [ИСПРАВЛЕНО]** — тавтологическая проверка удалена.
- **KI-H · LOW · [ИСПРАВЛЕНО]** — неиспользуемая переменная удалена.
- **KI-I · LOW · [ИСПРАВЛЕНО]** — мусорный каталог `./{tools...` удалён.
- **KI-J · LOW · [ИСПРАВЛЕНО]** — историческая справка в VERIFICATION_REPORT.md.
- **OPEN · этап 1** — hope_delta (ЖП) vs hope_points (ИдО): решить при движке, один эффект или два.

## Из gate-3 lynn-review ИдО (PASS, 24.06)

- **L1 · LOW · [ИСПРАВЛЕНО]** — `sposobnosti_vraga.payload.related` ретаргетирован `combat.boy` -> `adversaries.format_opisaniya`. Концепт «Способности врага» и экономика Ненависти/Решимости буквально описаны в `format_opisaniya` (владелец формата описания врага); `combat.boy` был лишь общим контекстом применения. Count-нейтрально (40 related->core сохранено). Правка в `build_solo_overlay.py` (envelope `sposobnosti_vraga`), JSON перевыведен ребилдом; 49 ИдО-карт ре-штампованы.

## Из gate-3 lynn-review КВ (PASS, 24.06)

- **KV-1 · INFO · [задокументировано, не дефект]** — provenance структурных чисел вне `combat.*`. Прогон `check_param_numbers` по всем 19 кластерам (гейт официально скоупится на `combat.*`) дал 9 «промахов» в 8 не-combat картах, ВСЕ книжно-верны: 6× `rewards_virtues.osobennosti_*` (`count:6` == `len(cultural_virtues)`), `hero_creation.cultures` (`count:6` == число `culture_ids`), `hero_creation.distinctive_features_intro` (`choose_per_culture:2`/`options_per_culture:8` — книга: «Выберите два… из перечисленных», у всех 6 культур ровно по 8). Это селекторные/структурные числа: верны внутренней консистентностью + книгой, но не трассируются в собственный `source_text`. Задокументированная граница `combat.*`-скоупного гейта, не ошибка транскрипции.
- **KV-2 · INFO · [не правлено, движку безразлично]** — `treasure.volshebnye_nagrady.notes`: прозо-глосса «знак успеха → `checks.feat_die_values`» неточна (путает d6-«знак успеха» с Костью испытания). Машинное ребро `feat_die_values` ВЕРНО — оно для порога Пробивания «9+ на Кости испытания». Косметика подписи, движок не затрагивает.
- **KV-3 · INFO · [косметика]** — нейминг токенов-аннотаций: `current_hope` (`shadow.bally_teni`) vs `hope_current` (`conditions.miserable`). Семантика идентична, движком не потребляется.
- **KV-4 · LOW · [ИСПРАВЛЕНО 24.06]** — застрявшая ОШИБОЧНАЯ команда `mark_verified.py … --id-prefix kv.` в трёх хэндоффах (`HANDOFF_KV_GATE2A_RECONFIRM.md`, `STARTER_KV_GATE2A_FIXES.md`, `HANDOFF_KV_GATE2A_FIXES.md`). `kv.` строит eligible-префикс `kv.mechanics.kv.` = 0 eligible → штампует НИЧЕГО (молча). КВ-штамп выполнен правильно беспрефиксной командой (`eabd9d1`: flipped 140 / already 49); устаревшие команды поправлены на беспрефиксный путь + эрратум.
