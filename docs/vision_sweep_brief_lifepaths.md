# vision-sweep — бриф сессии 2 (гейт 2 auto-verify v1, ADR-001)

TASK: поячеечная сверка 8 JSON в `content-packs/kv/lifepaths/` со сканами
«Жизненных путей героев».
MODE: VERIFY ONLY — правила и формат отчёта идентичны docs/vision_sweep_brief.md
(прочитай его первым), дельты ниже.

## Дельты против брифа сессии 1
- Сканы: `/mnt/project/Жизненные_пути_героев_на_рассылку_03_04_2026.pdf` — ZIP,
  распакуй, страницы 1–12. Текстовый слой НЕ открывать (циркулярность).
- **lifepath_cultures** (стр. 3): 6 строк, порядок культур.
- **lifepath_backgrounds** ×6 (стр. 4–9): на каждую строку — заголовок, ПОЛНЫЙ текст
  предыстории дословно, СИЛА/СЕРДЦЕ/РАЗУМ (числа!), любимый навык, оба качества.
- **lifepath_events** (стр. 10–11): глиф Ока у первой строки, руны — у последней;
  ярлык «…стал», текст эффекта (чёрный) и цитата (красный курсив) — проверь, что
  граница `effect_text`/`quote` в JSON проходит ровно по смене начертания на скане;
  все числа эффектов (шрам 1, ±опыт 10/5, ±ЦЧ 1, ±братство 1, ±Выносливость 2,
  ±Парирование 1, +Надежда 2, ±образ жизни 1, +Око 2, «1 за 11»).
- Поля `effects` и `engine_note` со сканом не сверяются (аннотации движка).
- Выход: `vision_sweep_report_lifepaths.json`, формат тот же.
