# DEFERRED — канонический реестр отложенной работы

Единственный источник правды по работе, сознательно отложенной на будущие этапы.
Памяти и разрозненных `HANDOFF_*.md` недостаточно (дрейфуют между сессиями); этот
файл в репо — нет.

## Правило-гейт (см. brodyazhnik-architecture-v1.md, §9 roadmap)

- Критерий выхода этапа N **не выполнен**, пока в этом реестре открыт пункт с
  `DUE ≤ N`.
- Стартер каждой сессии этапа **обязан** эхо-вывести открытые пункты своего этапа.
- Новый отложенный пункт заносится сюда **в той же сессии**, что и решение отложить.
- Закрытие: пункт переносится в раздел «Закрыто» с коммитом, который его закрыл.

`DUE` — самый ранний этап/гейт, обязанный пункт закрыть. Привязанные к этапу
пункты также проставлены инлайн в roadmap-строках архитектурного документа, чтобы
их нельзя было не встретить при старте этапа.

---

## Открытые

### HC1 — Создание героя / жизненные пути
- **Статус:** open
- **DUE:** Этап 4 (онбординг); вероятно нужно в Этапе 3 (лист героя).
- **Что:** Движкового потребителя у создания персонажа нет. 8 верифицированных
  таблиц жизненных путей (Stage 0) не читаются ничем; герой собирается руками
  (тестовый). Нужен модуль: культура + призвание + броски по путям →
  стартовый `HeroState`.
- **Почему отложено:** Критерий выхода Этапа 1 («CLI-прохождение путешествия без
  LLM») явно «на тестовом герое» — генерация не требовалась. Онбординг документ
  относит к Этапу 4.
- **Где живёт:** таблицы — `content-packs/kv/` (lifepath); состояние —
  `engine/src/hero/state.ts`; модуля `engine/src/creation/` пока нет.
- **Блокирует:** «первое полное приключение» (Этап 4), если приключение должно
  начинаться с генерации героя, а не с готового.

### FV1 — Фаза первых залпов (combat)
- **Статус:** open
- **DUE:** Этап 4 (доска боя) — либо раньше, как только разыгрывается соло-дальний
  бой.
- **Что:** `runCombat` по явному скоупу melee-only; enum `CombatPhase.first_volleys`
  есть, драйвера фазы нет. Число залпов (`pre_melee_volleys`) — scene-determined
  (зависит от дистанции, фикс-таблицы в паке нет).
- **Почему отложено:** Tact 5b явно ограничил скоуп мили-раундами; первые залпы и
  внезапность вынесены за него.
- **Где живёт:** `engine/src/combat/runCombat.ts` (строка о скоупе),
  `engine/src/combat/types.ts` (`CombatPhase`); родитель манёвра —
  `content-packs/kv/mechanics/manevrennaya_poziciya.json` (`pre_melee_volleys`).
- **Блокирует:** полный дальний бой; pre-melee часть манёвренной позиции.

### MX1 — Advance-бонус на выход вне манёвр-выхода (мелочь)
- **Статус:** open (низкий)
- **DUE:** Этап 2 или 4 — только если нарратив это упрёт.
- **Что:** Манёвр-выход уже тратит `pendingRangedBonusDice`. Остаётся узкая щель:
  применение Advance-бонуса при обычном (core) дальнем выходе из боя.
- **Где живёт:** `engine/src/combat/exit.ts` (rangedExit),
  `engine/src/combat/round.ts` (pending).
- **Блокирует:** ничего критичного; косметика полноты §3.7.

### TR1 — Сокровища / магические предметы
- **Статус:** open (выявлено coverage-аудитом)
- **DUE:** Этап 4 (награды приключения) + конверсия сокровищ в Фазе братства.
- **Что:** Домен `treasure` (8 карт: сокровища, знаменитое оружие/броня, проклятые
  предметы, клады, дивные артефакты, создание драгоценности, волшебные награды,
  список сокровищ) — нулевое потребление движком. Подсистема наград/предметов не
  построена.
- **Где живёт:** `content-packs/kv/mechanics/treasure/*`; потребителя в `engine/` нет.
- **Блокирует:** полноценные награды за приключение (этап 4); экономику Фазы
  братства (вместе со `standard_of_living`).

### PT1 — Задачи покровителей
- **Статус:** open (выявлено coverage-аудитом)
- **DUE:** Этап 4 (онбординг/приключение) — покровитель как рамка задания.
- **Что:** Таблицы `kv.solo.patron_tasks.*` (cirdan, gandalf, balin, bilbo,
  gilraen, tom_and_goldberry, …) не катятся движком. Механика выдачи задач
  покровителем не реализована.
- **Где живёт:** `content-packs/kv/tables/solo/patron_tasks.*`; потребителя нет.
- **Блокирует:** рамку «покровитель даёт задание» в приключении (этап 4).

### SD1 — Под-таблицы деталей сцен (оракул)
- **Статус:** open (выявлено coverage-аудитом)
- **DUE:** Этап 2 (нарративный цикл сцен) — когда движок катит элаборацию сцены.
- **Что:** `kv.solo.scene_details.*` (despair, terrible_misfortune, bad_choice,
  mishap, chance_meeting, short_cut) — второй уровень оракульного броска,
  движком не катится. По принципу «оракулы — механика движка» эти под-таблицы
  должны бросаться движком при раскрытии родительской сцены.
- **Где живёт:** `content-packs/kv/tables/solo/scene_details.*`; оракульный
  адаптер катит верхний уровень, под-таблицы — нет.
- **Блокирует:** полноту оракульной элаборации сцен в нарративном цикле (этап 2).

### LT1 — lore-чанки + tone.md (нарративный контент-долг)
- **Статус:** open (вынесен ADR-003 из Stage 0)
- **DUE:** Этап 2 — под собственные гейты (стоп-листы regex, golden, судья sec 0.7).
- **Что:** pack-layout sec 3 перечисляет `lore/` (RAG: Эриадор/регионы/покровители) и
  `tone.md` (регистр, голоса культур, стоп-лист пака); ADR-003 отложил их из Stage 0 в
  Stage 2; в паке их ещё нет. Слоты под них уже заведены в 2.1: `lore` →
  `NarrativePackage.lore_chunks`, `tone.md` → слоты `{{...}}` в `prompts/keeper.system.v0.md`
  и `VK_ADDENDUM_SLOT` в `evals/src/antislop.ts`. Наполнение — чаты 2.2/2.3.
  Сюда же — **калибровка длины по типу сцены** (§2.3.4: путь 400–800 / встреча
  800–1500 / раунд 300–600, + маппинг 5 `SceneKind`→диапазон): параметр `tone.md`,
  калибруется в 2.3; контракт-тип несёт только форму `length_target` (без чисел).
- **Почему отложено:** потребитель — Stage 2; verified-гейты для прозы/RAG/тона —
  инфраструктура Stage 2; писать контент раньше гейтов запрещено (ADR-003).
- **Где живёт:** `docs/ADR-003-lore-tone-stage2.md`; будущие `content-packs/kv/lore/` +
  `content-packs/kv/tone.md`; манифест расширяется бампом `pack_version`.
- **Блокирует:** критерий выхода Stage 2 (судья ≥ 80) — промпт/few-shot/eval опираются
  на `tone.md`+`lore`.

### RV1 — Независимый image-ревью 4 карт КВ
- **Статус:** open (content-discipline, не roadmap-этап) — независимый структурный
  проход выполнен (чат 2.1.d, `docs/RV1_REVIEW.md`: все 4 карты PASS по внутренней
  достоверности); остаётся image-часть.
- **DUE:** Content-ревью по ADR-001, до доверия `verified:true` этих карт в
  прод-загрузке.
- **Что:** Author-side sweep прошёл ранее. Независимый reviewer-проход 4 карт (вестник
  `hero_creation.calling_messenger` + 3 карты §3.7: `manevrennaya_poziciya`,
  `manevrennaya_poziciya_dalniy_boy`, `prodvinutsya`) выполнен в чате 2.1.d
  (`docs/RV1_REVIEW.md`) — все 4 PASS по внутренней достоверности. **Остаётся** image-
  сверка `source_text` по страницам (kv_core 44; kv_core 140–141; IdO 17) — нужны
  локальные изображения у владельца (в репо сканов нет); + ГЕЙТ-2b на
  `shadow.ispolzovanie_izyanov` (косвенно держит `wandering`-связку вестника).
- **Где живёт:** `content-packs/kv/mechanics/` (эти 4 карты); процесс —
  `docs/ADR-001-auto-verify.md`.
- **Блокирует:** строго — доверие к `verified:true` этих карт; не блокирует
  движок.

---

## Закрыто

(пусто)
