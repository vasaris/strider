# Gate-2a RE-CONFIRM @≈0becd6b — КВ-ядро (F1/F2/F3 + F4) — **PASS**

**Свежая НЕ-АВТОРСКАЯ re-confirm сессия** (ADR-001, шаг 2 gate-2a). Эти 5 карт я **НЕ строил и НЕ правил** — правки применены авторской сессией (F1/F2/F3/J5 @`636171d`, F4 @`0becd6b`). Задача: подтвердить, что приземлённые правки **корректны** и изменённые `parameters`/рёбра **не вводят в заблуждение**. **REVIEW-ONLY:** JSON/скрипты не тронуты, `verified` не тронут (его ставит только `mark_verified.py` после gate-3).
Архив пришёл **без `.git`**; коммит-хэши — по HANDOFF (≈`0becd6b`), не перевычислялись. Семантика вынесена **по книге** (греп источника), не по аргументу автора и не на доверии к confirm-скрипту.

---

## 1. Механические гейты (6/6 PASS)

```
validate.py                    OK: 222 files pass schema + structural validation
independent_check.py           cells checked: 676; failures: 0
check_param_numbers.py         OK: every parameter number traces to source_text (9 combat.* cards)
check_determinism.py           OK: content byte-identical across 222 files (stamp ignored; working tree untouched)
confirm_kv_gate2a_fixes.py     KV gate-2a re-confirm: PASS F1+F2+F3 ok
verify_gate2b_training_cost.py training_cost gate-2b: 18 numbers checked … RESULT: PASS (0 mismatches)
```

confirm-скрипт **неваккумный** (проверено чтением): F1 требует `eye_on_feat_die` **И** отсутствие `success-sign`/`знак успеха`; F2 — точное равенство строки; F3 — наличие 1+4 целей. Зелёный не является no-op.

---

## 2. Семантический re-read 5 карт (по книге)

### F1 — `eye.bditelnost_oka`: **PARTIAL → SUPPORTED** ✓
- `growth.eyes_gaze.trigger = "eye_on_feat_die out_of_combat"`. Референт «знака» = **Око**, подтверждён **независимо тремя путями по `source_kv/kv_core.txt`**:
  1. **Самоопределение книги:** «Знак — это наихудший результат при броске. Если на Кости испытания выпадает знак, результат броска данной кости равен 0» → голый «знак» = Око (наихудший = 0).
  2. **Греп-конвенция 6/6:** все «выпада*/выпал знак» (3+1+2) = Око (самоопределение + miserable-автопровал); **0** контрпримеров «знак успеха». Контр-термин «знак(а/и) успеха» — **24 вхождения, всегда квалифицирован**, всегда = ресурс степени успеха на Костях успеха у цифры 6 (тратится только при успехе).
  3. **Result-independence:** сама фраза ВЗОР ОКА «…выпадает знак, **независимо от результата проверки**» несовместима с ресурсом успеха (тот релевантен лишь при успехе) → поведение Ока.
- Глиф появляется только на Кости испытания (d12), не на Костях успеха → `eye_on_feat_die` мехточен (зеркало вокабуляра `conditions.miserable`).
- Модификаторы согласованы с **нетронутым** `summary`: `amount:1` («увеличивается на 1»), `tense_situation:two_or_more` («на 2 или более»), `safe_place:0` («может не вызвать… вовсе»). 
- `notes` исправлены: больше **не** указывают на «знак успеха ✶»; корректно фиксируют Око-резолюцию. **Не вводят в заблуждение.**
- 5 рёбер корректны и существуют (`oko_mordora`, `presledovanie`, `valour_wisdom.doblest` [Доблесть≥4], `shadow.bally_teni` [рост Тени], `checks.feat_die_values` [глиф Кости испытания]).

### F2 — `shadow.bally_teni`: **PARTIAL → SUPPORTED** ✓
- `miserable_effect = "eye_on_feat_die = auto_failure"` — **байт-в-байт зеркало** `conditions.miserable.parameters.effect`. Источник (греп): «выпадение знака на Кости испытания считается провалом, независимо от ЦЧ и фактического результата броска» = Око.
- Старое `success_sign_on_feat_die_counts_as_failure` было самопротиворечиво (знаков успеха на Кости испытания не бывает); устранено. **Не самопротиворечив, не вводит в заблуждение.**
- 6 рёбер корректны (как и в gate-2a).

### F3 — под-линкинг (рёбра OK) ✓
| карта | `related` | проверка |
|---|---|---|
| `checks.who_rolls` | `[checks.assistance]` (n=1) | set-равно ожиданию; цель существует; не висит. Карта операционно вводит контекст Помощи — ребро в правильную карту. |
| `checks.which_ability` | `traits.navyki`, `traits.boevye_umeniya`, `valour_wisdom.doblest`, `valour_wisdom.mudrost` (n=4) | set-равно; все 4 существуют; не висят. Ровно отображают способности 3 типов проверок из `summary` (навык / боевая / Тени). |
| `checks.retry` | `[]` | **пусто защитимо**: самодостаточное мета-правило «одна попытка; повтор иным навыком с разрешения хранителя» — опц. ссылки (`procedure`/`which_ability`) не обязательны (WEAK). |

### F4 — `fellowship_phase.struktura_fazy_bratstva`: **SUPPORTED, числа gate-2b-closed** ✓
- `training_cost.rows` точно воспроизводит scan-evidence `gate2b_evidence/MANIFEST.md` (фолио 119): C1=1–6, C2=`null`/2/3/4/5/6, C3=4/8/12/20/26/30; **привязка построчно** (навык/умение N и Доблесть/Мудрость N делят цену строки; у Доблести/Мудрости нет уровня 1 → `null`). 18 ячеек (6×3), verifier PASS 0 mismatches; негативный контроль на text-layer-поток `[4,2,8,3,12,4,20,5,26,6,30]`.
- `notes` корректно метят **C1 как пиктограммную scan-only колонку** («текст-слой их роняет»).
- Таблице не противоречит `summary` (он ссылается на «таблицу стоимости» без выдумывания цен) ни `source_text` (числа из скана — домен gate-2b, не деградированного слоя). Не вводит в заблуждение.

---

## 3. Reviewer-гигиена

- **Verified-sweep:** kv_core — **140 карт, все `verified:false`, 0 ошибочно `true`** (инвариант handoff соблюдён). 82 `verified:true` в паке = `lifepaths.*`/`mechanics.solo.*` (ИдО), простэмплены своим прежним gate-3 — **вне скоупа**, не тронуты.
- **Write-check:** ни одной записи в JSON/скрипты; `check_determinism` повторно — «working tree untouched», byte-identical 222.

---

## 4. ВЕРДИКТ — **PASS**

- 2 ex-PARTIAL (`bditelnost_oka` F1, `bally_teni` F2) теперь **SUPPORTED** — референт «знака» = Око подтверждён независимо по источнику (не на доверии методу автора).
- F3-рёбра **корректны** (1+4 цели существуют, не висят; `retry=[]` защитимо).
- F4-таблица **согласована** со scan-evidence (MANIFEST ф.119) и verifier; C1 верно помечена scan-only.
- Content-фиделити КВ-ядра: **140/140 SUPPORTED · 0 PARTIAL · 0 UNSUPPORTED** на этом HEAD.

**Передача:** → **gate-3 (lynn-review)**, ещё одна свежая НЕ-авторская сессия (`docs/lynn_review_brief_stage0_session*.md`) → затем `mark_verified.py --id-prefix kv.` → **выход Stage 0**. `verified` НЕ трогать до gate-3.

---

## 5. Самооценка (110/100, честные слабые места — не блокеры)

1. **F1/F2 без скана.** Re-confirm не циркулярен: референт переустановлен **из первичного текста** (греп 6/6 + самоопределение + result-independence), а не из аргумента автора. Остаточный риск: доверие к тому, что gate-1 рез сохранил **прозу вокруг** уронённого глифа дословно — ровно то, что гарантирует `independent_check` (676/0) и врезка-по-якорям; роняется только сам глиф (виден как ctrl-байт). Приемлемо.
2. **F2-зеркало — дублированный строковый литерал** в двух картах (`bally_teni.miserable_effect` ↔ `miserable.effect`). Сейчас консистентны; но `check_param_numbers` стережёт лишь **числовые** дубли, не строковые → риск дрейфа при правке одной карты без другой. Это **forward-looking** замечание для Stage-1 (движок читает оба), **не** content-дефект и **не** блокер — зеркало является явным намерением автора.
3. **F4 — числа из скана, не перерастеризованы мной.** Сверка card ↔ MANIFEST ↔ verifier (все согласны), но я **не** открывал PDF-фолио сам (это домен vision-сессии по ADR-002 §2b; настоящий PDF — у Ивана). Полностью независимый ре-скан F4 — за рамками этого механического re-confirm. Честно: F4-числа приняты на консистентности evidence-цепочки, не на повторной vision-сверке.
4. **`who_rolls→assistance`:** HANDOFF метит STRONG; по-моему ближе к **MODERATE** (Помощь — смежное правило, не строго «вводимое» этой картой). На **корректность** не влияет: цель верна, существует, тематически точна, не вводит в заблуждение. Лейбл силы — не блокер.
