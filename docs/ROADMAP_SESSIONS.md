# ROADMAP_SESSIONS — план по сессиям до конца проекта

Канонический разбивочный план: этапы из `brodyazhnik-architecture-v1.md` §9 →
сессии (чаты) → деливераблы (подчаты). Живёт в репо (план в чате дрейфует, как и
память; этот файл — нет).

**Точность вперёд (честно):** ближнее (Stage 2–3) — детально и планируемо;
Stage 4 крупнее и грубее (зависит от исходов 2–3); Stage 5 эмерджентный (ведётся
твоими транскриптами), дроблю только по якорям. План **пересобирается на каждом
хендоффе**: выполненный чат отмечается, дальний этап переразбивается, если
реальность разошлась.

**Единицы:**
- **Чат N.x** = одна сессия: старт с zip+handoff, финиш с handoff перед
  насыщением контекста (сигнал 🔴 ПОРА В НОВЫЙ ЧАТ).
- **Подчат N.x.y** = один трёхтактный деливерабл: скелет → «го» → реализация+тесты+зип.

**Привязка стартера (см. архитектурный документ, гейт отложенного):** стартер
каждой сессии указывает, какой чат/подчат этого файла он закрывает, и эхо-выводит
открытые пункты `DEFERRED.md` с `DUE ≤ текущего этапа`. Этап не закрыть, пока такой
пункт открыт.

Статусы: ✅ done · ▶ in-progress · ⬜ pending.

---

## ✅ STAGE 0 — Контент (закрыт)
Пак `kv` с `verified` таблицами: 222 файла. Закрыт.

## ✅ STAGE 1 — Движок (закрыт; коммит b575dcf)
Все механические подсистемы до Фазы братства + coverage-аудит. Подтверждение —
`docs/STAGE1_COVERAGE.md`. Критерий выхода (CLI-путешествие без LLM) зелёный.

---

## ⬜ STAGE 2 — Нарратив
Критерий выхода: CLI-сцена с прозой, судья ≥ 80. ~4 сессии, эластично.
**Гейт DUE ≤ 2 (нельзя пропустить):** `LT1` (tone.md + lore + детерм. гейт, чат 2.2)
обязан закрыться до выхода Stage 2. `SD1` — механика сделана в Stage 1 (ложный
deferred-вердикт исправлен); открытый остаток — сюрфейсинг детали в `oracle.detail`,
чат 2.4. `RV1` — content-gate (не roadmap-этап), доводится по изображениям книги.
**Порядок Stage 2 (зависимости):** корректировки → LT1 → few-shot/голоса → судья/golden
→ цикл 2.4 (вкл. SD1-сюрфейсинг). ADR-003: тон-несущую прозу (few-shot/голоса) нельзя
раньше тон-гейта — её вход `tone.md` (LT1).

- ✅ **Чат 2.1 — Нарративный контракт + скелет промпта** (сдан; приём Ивана)
  - ✅ 2.1.a Контракт `движок → Хранитель`: тип `orchestrator/src/contract.ts` (ASCII,
    content-clean) + спека `docs/NARRATIVE_CONTRACT.md` (4 правила + явный «запрещено
    выдумывать»). Слоты SD1 (`oracle.detail`) и LT1 (`lore_chunks`) заведены.
  - ✅ 2.1.b Системный промпт Хранителя v0 (скелет): `prompts/keeper.system.v0.md`
    (русский; слоты `{{...}}` под tone.md/lore — LT1)
  - ✅ 2.1.c Anti-slop: машинный seed `evals/src/antislop.ts` + чек-лист
    `docs/ANTISLOP_CHECKLIST.md`. *(ревьюер: seed живёт в `evals/`, не docs/ — sec 8/2.5)*
  - ◐ 2.1.d **RV1** (DEFERRED): независимый структурный проход 4 карт выполнен
    (`docs/RV1_REVIEW.md`, все PASS); image-сверка остаётся open (нужны сканы у владельца)
  - ✅ Хендофф: чат 2.1 закрыт коммитом; остаётся RV1 ИдО-сторона (ждёт образов ф.17 у владельца)
- ▶ **Чат 2.2 — Корректировки реестра/аудита + LT1 (tone.md + lore + детерм. гейт)**
  - ✅ 2.2.a SD1 — ложный deferred-вердикт исправлен (docs-only): `scene_details`
    катятся в journey с Stage 1 (`journey/scene.ts:42-47`); остаток (сюрфейсинг) → 2.4.
    Правки `DEFERRED#SD1`, `STAGE1_COVERAGE` (+метод-сноска про `listByType`),
    +`inspiring_sight` (7 таблиц). Engine/golden не тронуты.
  - ◐ 2.2.b **LT1** tone.md: **скаффолд сдан** (драфт+frontmatter-гейт+ревью-рекорд,
    `e8d5e73`); **контент ждёт активации Иваном** (проза+verified+move)
  - ◐ 2.2.c **LT1** lore/: **скаффолд сдан** (схема+5 драфт-шаблонов, `e8d5e73`);
    **контент ждёт активации** (пересказ из книг+source страницы+verified+move)
  - ✅ 2.2.d Детерм. гейт LT1: `evals/lt1gate.ts` (enabler) + `VK_ADDENDUM`-лоадер +
    манифест-машинерия 0.2.0 + pending-стейджинг — сдано (`e8d5e73`)
- ⬜ **Чат 2.3 — Few-shot корпус + голоса культур** (вход = активированный tone.md; ADR-003)
  - 2.3.a Few-shot сцены против стоп-листов (ведущий звук/запах/осязание)
  - 2.3.b Голоса культур (хоббит ≠ гном ≠ дунэдайн; одна речевая привычка на персонажа)
  - 2.3.c Калибровать токен-прокси (`CHARS_PER_TOKEN`) против токенайзера **нарративной
    модели** (Anthropic/Claude), не генерик; бюджет lore-RAG из block-warn → точный
- ▶ **Чат 2.4 — расщеплён на plumbing (контент-независимо) + live (после tone.md)**
  - **plumbing (сделано):**
    - ◑ 2.4.p1 **SD1 Fork A** (engine): полная detail-строка в scene-`JourneyEvent`
      (аддитивно; 389 зелёные + golden `dark-1` byte-identical без `-u`)
    - ◑ 2.4.p2 Структурный `orchestratorPackageProvider` (`orchestrator/provider.ts`):
      `EngineTurnResult → NarrativePackage`, SD1-строка → `oracle.detail.row`; фикстурные
      тесты; **Опция 2 — без кросс-пакетного импорта**
    - ◑ 2.4.p3 Инъекция в harness-шов (структурный engine-derived провайдер) + RECONCILE-чек-лист
  - **live (после `tone.md` + workspace):**
    - 2.4.L1 Реальные engine-типы (RECONCILE на workspace) + `AnthropicKeeper`
    - 2.4.L2 LLM-судья (5 осей + замена `anti_slop`), калибровка к ≥ 80
    - 2.4.L3 Suite-раннер (5–10 golden-сценариев, агрегат pass-rate ≥ 80) + цикл хода
    - 2.4.L4 End-to-end CLI-сцена; критерий выхода + handoff + проверка гейта

### Stage 2 — состояние после активации LT1-tone
**Закрыто (контент-независимо):** контракт+промпт-скелет (2.1), anti-slop seed +
completeness, eval-харнесс скаффолд, **2.4 plumbing** (SD1 Fork A + структурный provider +
инъекция), LT1 скаффолд+гейт+драфты.
**LT1-tone активирован** (`tone.md` + `tone.stoplist.json` в живом паке, сайдкары, 0.1.0;
`evals` читает `VK_ADDENDUM` из живого сайдкара). Тон-сторона live-2.4 **разблокирована**.
**Сделано:** тон-судья **плумбинг** (`76421b8`, LlmJudge/error-verdict/short-circuit/pluggable-aggregate),
**few-shot v1** (`013dce0`, G1–G6+B1–B7, догфуд PASS), **LT1-tone активирован** (`f6beb0f`).
**▶ Калибровка тон-судьи — РАННЕР СДАН, ждёт прогона Ивана:**
- Раннер: `evals/calibrate.mts` + `cases.ts` + `anthropicLlmClient.ts` (env-гард, model-config,
  Zod-гейт, prompt-caching, raw→`calibration-report.json` gitignored). Оффлайн-тесты не задеты.
- **Ключ-путь (а):** ключ ТОЛЬКО в keyed-вкладке Ивана, никогда в процесс агента. Команда:
  `cd evals && npx tsx calibrate.mts`.
- **Первый прогон = ДИАГНОСТИКА.** Иван гоняет сам, несёт сырой `calibration-report.json` +
  таблицу на разбор (разбор — во внешнем арх-чате, не здесь).
- 🔴 **До разбора НЕ трогать:** `judge.system.v0.md` (рубрика), `cases.ts`, floor агрегата, порог.
  Если B5–B7 проходят высоко → чинить рубрику, не порог/regex.
**Всё ещё gated:** полный цикл хода + suite-раннер (5–10 golden, агрегат ≥80) + `AnthropicKeeper` —
на **workspace** (открыватель Stage 3) + **lore-активацию** (LT1 lore → 0.2.0).

---

## ⬜ STAGE 3 — PWA
Стек зафиксирован (Next.js App Router PWA, Tailwind, Postgres/Supabase, Anthropic
API). Выход: играбельная сцена в браузере. ~4 сессии.

**Открыватель Stage 3 (конкретный драйвер, не гипотеза):** root **workspace**
(`@brodyazhnik/engine`/`orchestrator`/`evals` + `app/`). Триггер пришёл в 2.4-plumbing —
первая реальная кросс-пакетная зависимость (orchestrator маппит engine-вывод;
evals-harness зовёт orchestrator). До workspace они связаны структурными seam'ами
(`EngineTurnResult`, harness-alias); workspace заменяет их реальными типами и снимает
RECONCILE-долг. В Stage 2 workspace не тянем (поздне-этапная работа).

- ⬜ **Чат 3.1 — Каркас + API-контур**
  - 3.1.a Scaffold + PWA manifest/service worker
  - 3.1.b Серверный роут: движок + Хранитель за чистой границей
  - 3.1.c Supabase-схема персистентности (сессия/герой)
- ⬜ **Чат 3.2 — Лента + кости + действия**
  - 3.2.a Лента хода · 3.2.b Отображение бросков (сид виден) · 3.2.c Ввод → ход → рендер
- ⬜ **Чат 3.3 — Трекеры + лист героя**
  - 3.3.a Трекеры (Выносливость/Надежда/Тень/Око/состояния)
  - 3.3.b Лист героя из персистентного состояния
  - 3.3.c Round-trip персистентности
  - ⚠ Развилка: тянуть ли **HC1** в Stage 3 ради листа героя, или держать готового/импортного (по умолчанию — держать)
- ⬜ **Чат 3.4 — Играбельная сцена + закрытие**
  - 3.4.a Сцена из Stage 2 в браузере · 3.4.b PWA install/offline · 3.4.c Критерий выхода + handoff

---

## ⬜ STAGE 4 — Приключение
Выход: **ты проходишь первое полное приключение.** Сюда садятся HC1/FV1/PT1/TR1.
~6 сессий, оценки грубее.

- ⬜ **Чат 4.1 — HC1 создание героя: скелет + культуры/призвания**
  - 4.1.a Скелет конвейера (культура→призвание→характеристики→навыки→снаряжение→состояние)
  - 4.1.b Карты культур/предысторий · 4.1.c Сборка `HeroState` + тесты
- ⬜ **Чат 4.2 — HC1 жизненные пути + снаряжение**
  - 4.2.a Броски по путям · 4.2.b Лоадаут: таблицы оружия/брони (домен equipment) · 4.2.c Golden героя
- ⬜ **Чат 4.3 — FV1 фаза первых залпов**
  - 4.3.a Драйвер залпов (число от дистанции) · 4.3.b Интеграция в `runCombat` · 4.3.c Стыковка манёвра + golden
- ⬜ **Чат 4.4 — TR1 сокровища + эффекты Фазы братства**
  - 4.4.a Сокровища (клады/магич./знаменитое — приобретение+opaque) · 4.4.b `standard_of_living` + ЭФФЕКТЫ начинаний · 4.4.c тесты
- ⬜ **Чат 4.5 — PT1 покровители + генератор скелетов**
  - 4.5.a Задачи покровителей · 4.5.b Генератор скелетов приключения · 4.5.c Онбординг (создание→покровитель→сцена)
- ⬜ **Чат 4.6 — Доска боя (UI) + первое полное приключение**
  - 4.6.a Доска боя в браузере (Stage 3 + FV1)
  - 4.6.b End-to-end: онбординг→путешествие→бой→совет→Фаза братства
  - 4.6.c Критерий выхода + handoff

---

## ⬜ STAGE 5 — Полировка (эмерджентный; якоря)
Выход: стабильная версия для инвайтов.

- ⬜ **Чаты 5.x — итерации по транскриптам** (N сессий, заранее не перечислимы): транскрипт → правки топ-проблем (слоп, краевые случаи механики, UX), одна сессия = один прогон
- ⬜ **Чат 5.campaign — кампания-персистентность**: цепочки заданий, рост Тени между приключениями, save/resume
- ⬜ **Чат 5.final — стабилизация**: регресс-прогон, перф, чек-лист приватного контура (ноль публичных ссылок/монетизации), финальный coverage-аудит

---

## Конверт

Stage 2 ~4 + Stage 3 ~4 + Stage 4 ~6 + Stage 5 ~3 якоря = **~17–20 сессий** до
«стабильно для инвайтов», плюс непредсказуемые итерации Stage 5. Чем дальше — тем
грубее. Stage 2–3 ведутся по плану почти как есть; Stage 4 пересобрать после 2–3;
Stage 5 формируется игрой.

## Сопровождение

Обновлять на каждом хендоффе: отметить выполненный чат (⬜→✅), при расхождении —
переразбить ближайший незакрытый этап, синхронизировать новые DEFERRED-пункты.
